# Implementation Plans

Generated by the improve skill on 2026-08-28, from a full audit at commit
`700de9a`. Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update your row when done.

These plans are NEW findings — they do not duplicate `docs/HARDENING_TRACKER.md`.
When a plan lands, consider adding a one-line entry to the tracker's ledger so
the two documents stay consistent (the tracker remains the repository-owned
source of truth for the hardening program).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Stop leaking a Chrome per sync; bound quit teardown | P1 | S | — | DONE |
| 002  | Characterize the sync/notification pipeline with tests | P1 | M | — | DONE |
| 003  | Never mark an item "seen" before the user was told | P1 | M | 002 | DONE |
| 004  | Manual sync merges into the cache instead of overwriting | P1 | M | 002 (soft) | TODO |
| 005  | Gate debug dumps of authenticated pages; unlog username | P1 | S | — | DONE |
| 006  | Consent-gated updates + truthful release docs | P1 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **003 requires 002 DONE**: 003's red-green mechanism is flipping 002's
  characterization pins; without them the reorder of `syncNow()` is unverified.
- **004 soft-depends on 002**: no shared code, but 002 establishes the jsdom
  test patterns 004 reuses; 004 can run before 002 if needed.
- **001, 005, 006 are independent** of everything and of each other. 001 and
  005 both edit `playwright-login.service.ts` (different regions) — land
  serially to avoid merge noise.
- 006's `quitAndInstall` path benefits from 001's quit-timeout (noted in both
  plans) but does not require it.

## Audit findings NOT planned in this batch (still open)

Ranked leftovers from the 2026-08-28 audit, available for a next batch:

- **F4** — batch download's skip-if-exists check compares the raw UI name
  against files written with appended extensions; never matches, every batch
  re-downloads everything (`sigaa.service.ts:313-324` vs
  `http-scraper.service.ts:960-967`). Effort S.
- **F7** — course verification by `normalizedContent.includes(normalizedName)`
  passes Cálculo II for Cálculo I (`playwright-login.service.ts:488-494`).
  Effort S, needs a fixture pair first.
- **F5** — one write error rejects out of the batch loop, discarding all
  results (`http-scraper.service.ts:1034-1037`, caller `sigaa.service.ts:426`).
  Effort S.
- **F6** — expired-session recovery discards `getCourses()` result and
  null-derefs (`playwright-login.service.ts:359`, `:367`). Effort S.
- **F8** — renderer listener leaks: dashboard re-subscribes
  `onBackgroundSyncUpdate` per render, `document` click listener per render,
  modal overlay listener per open. Effort S.
- **F9** — `stopLoadingInterval()` called but never defined; loading timer
  runs forever (`src/pages/loading.ts:38`, `src/main.ts:57,67`). Effort S.
- **P1** — Modo Completo re-fetches every (immutable) news body every sync
  (`sigaa.service.ts:599-612`). Effort S-M.
- **P2** — ~5s of fixed sleeps + duplicated verification per course entry
  (`playwright-login.service.ts:462-517`). Effort S, MED risk (JSF timing).
- **P3** — monolithic `coursesWithFiles` blob: quadratic rewrite during sync,
  no quota handling. Effort M (partially mitigated by plan 004's clearer
  quota error).
- **D1** — lint ratchet unenforced: `"lint": "eslint ."` should be
  `eslint . --max-warnings=<current count>`. Effort: one line.
- **D2/D4** — README test instructions wrong (claims `npm test` runs live
  SIGAA tests); ROADMAP lists shipped features (dark mode, settings
  persistence) as unbuilt. Effort S.
- **T4** — five tests that cannot fail (locations in the audit report /
  `tests/integration/scraper.test.ts:131` etc.). Effort S.
- **T5** — `tests/e2e/app.spec.ts` tests are order-dependent. Effort S.
- **S5** — cookie domain match is unanchored `endsWith`
  (`http-scraper.service.ts:61`). Latent; 3-line hardening.
- **S6** — `quality.yml` has no `permissions:` block; `gitleaks-action`
  unpinned. (Workflows dir is agent-write-protected — owner applies by hand.)
- **DB1/DB2** — duplicated course-entry traversal (2 live copies, one under a
  stale "unused" comment); `course-detail.ts` re-derives the cache lookup six
  ways. Effort M each.
- **Direction options** (maintainer's call): render the already-parsed
  `file.date` + sort files reverse-chronologically (best value/line found);
  finish "Modo Backup"; search over the offline corpus; unify manual/background
  "what's new"; grades/activities read-only spike (after Fase 3 only).

## Evidence contributed to existing tracker tasks

- **BUG-004** (wire the Playwright download fallback): when implemented, it
  MUST NOT keep `download.service.ts:109-123`'s `new Function(<scraped onclick>)`
  execution — parse the `jsfcljs(...)` shape (regex already at
  `http-scraper.service.ts:861`) and reject non-matching scripts, and validate
  the `script` field at the IPC boundary; also add an `https://si3.ufc.br`
  origin allowlist before every `page.goto` with scraped/IPC URLs.
- **DATA-002**: `clear-all-data` handler (`electron/main.ts:257-268`) is a
  byte-copy of `logout` — clears neither `cache.json` nor `settings.json`;
  post-clear syncs see a warm baseline and notify nothing. Also sweep
  `userData` `debug_*.html` and `logs/` (see plan 005 maintenance notes).
- **CONC-001**: concrete race evidence — batch download replays per-course
  ViewState while background sync navigates the shared page to another course;
  `sigaa.service.ts:21-34`'s `busyCount` is incremented and never read.

## Findings considered and rejected

- **Delete `enterCourseHTTP` (~100 lines, zero callers)**: contradicts the
  recorded author decision to keep it pending `BUG-010` (it is the only
  reader of `this.userAgent`). Not re-auditable until BUG-010 resolves.
- **npm audit refresh**: still 5 high / 0 critical, identical shape to
  `DEP-001`'s 2026-08-09 baseline — nothing new to plan.
- **`npm run preview` is cmd.exe-only**: real, but one line in a future
  README pass; not worth a plan.
- **Wiring the `autoDownloadUpdates` setting to the app updater**: the setting
  governs course files despite its name; repurposing a persisted setting is a
  migration, deferred (noted in plan 006).
