Verdict: Not approved
Audited by GPT-5.6 Sol

## Summary

- O gate do repositÃ³rio, a cobertura configurada, a auditoria de dependÃªncias e o E2E sem credenciais passam.
- Cinco testes de quebra externos Ã  Ã¡rvore reproduziram cinco falhas funcionais nÃ£o cobertas pela suÃ­te.
- As falhas de maior impacto permitem atribuir conteÃºdo Ã  disciplina errada, rejeitam uma conta vÃ¡lida sem turmas e tornam materiais externos inacessÃ­veis.
- HÃ¡ ainda caminhos quebrados de retomada de sessÃ£o, download offline e encerramento, alÃ©m de dÃ­vidas de robustez e limpeza.

## Scope and coverage

- Escopo: repositÃ³rio inteiro no commit `d5bb393f3db8b107a5a21660bcf818f9f0f0ba85`, com inventÃ¡rio de 362 arquivos rastreados, excluindo deliberadamente `AUDIT.md` e `docs/audits/` conforme solicitado.
- Leitura integral: entrypoints e configuraÃ§Ã£o; contratos compartilhados; IPC; persistÃªncia; coordenaÃ§Ã£o de operaÃ§Ãµes; compatibilidade do portal; fluxos principais de login, sync, scraping, download, conta, notificaÃ§Ãµes e pÃ¡ginas do renderer.
- Leitura parcial orientada a risco: restante de `src/`, 87 arquivos de teste, estilos, documentaÃ§Ã£o, planos, tracker e workflows; foram aprofundados os mÃ³dulos grandes, fronteiras de confianÃ§a, fallbacks, contratos e caminhos citados pelos testes.
- NÃ£o lidos como conteÃºdo auditado: imagens/Ã­cones binÃ¡rios, artefatos gerados, `AUDIT.md` e qualquer relatÃ³rio apagado de `docs/audits/`; nenhum histÃ³rico de auditoria foi consultado ou restaurado.

## Method

- Gate: `npm run quality` passou â€” typecheck, lint com 0 erros e 52 warnings, 65 arquivos Vitest, 733 testes aprovados e 5 ignorados.
- Cobertura: `npm run coverage` passou â€” 90,03% statements, 85,64% branches, 100% functions e 94,83% lines, mas o piso cobre somente os cinco mÃ³dulos crÃ­ticos declarados em `vitest.config.ts`, nÃ£o o repositÃ³rio todo.
- DependÃªncias: `npm audit --omit=dev --audit-level=high` e `npm audit --audit-level=high` retornaram 0 vulnerabilidades.
- LanÃ§amento: `npm run test:e2e` compilou e abriu o Electron; 39 testes passaram e os 3 que exigem credenciais reais foram ignorados. O scan axe passou nos temas claro e escuro.
- Tentativas de quebra: uma suÃ­te Vitest descartÃ¡vel fora do repositÃ³rio exerceu cÃ³digo de produÃ§Ã£o e obteve 5/5 falhas esperadas: nome de disciplina por prefixo, portal autenticado vazio, lote totalmente reaproveitado, bootstrap sem sessÃ£o e preservaÃ§Ã£o de URL externa.

## Top 3

### 1. Broken â€” `electron/services/playwright-login.service.ts:45`

O que estÃ¡ errado: `isExpectedCoursePage` usa `header.includes(normalize(courseName))`; por isso o cabeÃ§alho de â€œCÃLCULO IIâ€ Ã© aceito quando a turma esperada Ã© â€œCÃLCULO Iâ€. Impacto: uma falha silenciosa de navegaÃ§Ã£o JSF pode atribuir arquivos e notÃ­cias da turma II Ã  turma I, exatamente a corrupÃ§Ã£o entre disciplinas que essa guarda pretende impedir. CorreÃ§Ã£o: extrair do cabeÃ§alho o nome delimitado entre cÃ³digo e perÃ­odo e comparÃ¡-lo por igualdade normalizada; acrescentar casos de prefixo nos dois sentidos e nomes com algarismos romanos.

### 2. Broken â€” `electron/services/playwright-login.service.ts:383` e `electron/services/background-sync.service.ts:296`

O que estÃ¡ errado: `getCourses` transforma zero inputs/links em `SELECTOR_DRIFT`, embora o classificador, a fixture e o contrato aceitem um portal autenticado com zero turmas; mesmo apÃ³s corrigir isso, `structuralDriftCourses === courses.length` Ã© verdadeiro para `0 === 0`. Impacto: uma conta vÃ¡lida sem turmas falha na sincronizaÃ§Ã£o e pode acumular falsos eventos estruturais atÃ© persistir o kill-switch de incompatibilidade. CorreÃ§Ã£o: validar primeiro o landmark autenticado e retornar sucesso com `courses: []`; no background, sÃ³ aplicar a regra â€œtodas em driftâ€ quando `courses.length > 0`.

### 3. Broken â€” `electron/services/sigaa.service.ts:38`

O que estÃ¡ errado: o parser cria materiais externos com `url`, mas `toCourseFile` descarta a URL; `CourseFile` nÃ£o a modela e `course-detail.ts:293` renderiza apenas um Ã­cone inerte. Impacto: o usuÃ¡rio vÃª o material anunciado pelo professor, mas nÃ£o consegue abri-lo nem baixÃ¡-lo. CorreÃ§Ã£o: preservar uma URL validada no ramo `type: 'link'` do domÃ­nio/IPC e renderizar uma aÃ§Ã£o que passe pela polÃ­tica segura jÃ¡ existente de abertura externa.

## All findings

- Broken â€” `electron/services/playwright-login.service.ts:45`: comparaÃ§Ã£o por substring aceita uma turma diferente cujo nome apenas estende o esperado, permitindo mistura de conteÃºdo entre disciplinas.
- Broken â€” `electron/services/playwright-login.service.ts:383`, `electron/services/background-sync.service.ts:296`: portal autenticado sem turmas vira drift e a comparaÃ§Ã£o vazia do background tambÃ©m conta uma falha estrutural.
- Broken â€” `electron/services/sigaa.service.ts:38`, `shared/domain.ts:71`, `src/pages/course-detail.ts:293`: a URL de material externo morre na reduÃ§Ã£o parserâ†’domÃ­nio e a interface nÃ£o oferece abertura.
- Broken â€” `electron/services/playwright-login.service.ts:435` e `electron/services/playwright-login.service.ts:857`: os fluxos de turma e notÃ­cia ignoram a falha de `getCourses`; o primeiro dereferencia `context` nulo e ambos perdem `SESSION_EXPIRED`, impedindo o relogin correto.
- Broken â€” `electron/services/sigaa.service.ts:407`: mesmo com toda a fila jÃ¡ validada e marcada como `skipped`, o lote exige navegar no portal e devolve erro offline em vez do resultado local completo.
- Broken â€” `electron/main.ts:288`: â€œSairâ€ no tray seta `isQuitting` antes de `before-quit`, pula `sigaaService.logout()` e `logger.flush()` e pode deixar Chrome/sessÃ£o e logs sem teardown.
- Fragile â€” `electron/sigaa/portal-state-classifier.ts:49`: manutenÃ§Ã£o e acesso negado documentados caem em `UNKNOWN`/`SELECTOR_DRIFT`, podendo alimentar o kill-switch como se o HTML tivesse mudado.
- Fragile â€” `src/data/account-storage.ts:121` e `src/utils/notification-store.ts:94`: JSON sintaticamente vÃ¡lido, porÃ©m com forma errada, atravessa o storage sem validaÃ§Ã£o e pode quebrar gravaÃ§Ã£o de downloads ou operaÃ§Ãµes do painel de notificaÃ§Ãµes.
- Slop â€” `electron/services/playwright-login.service.ts:1`, `shared/ipc.ts:82`, `package.json:63`, `README.md:101`: um serviÃ§o de 1.119 linhas concentra navegaÃ§Ã£o, login, parsing, notÃ­cias e downloads; `autoSync` Ã© estado morto; `sqlite` Ã© dependÃªncia de produÃ§Ã£o sem import e o README afirma que ela armazena dados.

## Process

- CI: `quality.yml` separa gate, E2E e scanner de segredos; `release.yml` repete os gates antes da publicaÃ§Ã£o manual. Os fluxos estÃ£o coerentes com os scripts locais.
- Testes: a suÃ­te Ã© ampla e rÃ¡pida, mas os 5 testes descartÃ¡veis mostram lacunas em composiÃ§Ã£o entre camadas; cobertura percentual alta nÃ£o alcanÃ§a os mÃ³dulos onde quatro dessas falhas vivem.
- Lint: 52 warnings passam sem limite ou baseline, portanto o gate nÃ£o impede crescimento dessa dÃ­vida.
- ContribuiÃ§Ã£o: nÃ£o hÃ¡ `CONTRIBUTING.md`; regras importantes estÃ£o distribuÃ­das entre `AGENTS.md`, `CLAUDE.md` e `docs/agents/`, o que aumenta o custo de entrada fora das ferramentas que descobrem esses arquivos automaticamente.

## Previous audit

Esta Ã© a primeira auditoria. Nenhum relatÃ³rio anterior foi lido, restaurado ou consultado no histÃ³rico.

## Recommended order

1. Corrigir e cobrir a igualdade de turma, o estado de zero turmas e a preservaÃ§Ã£o/abertura de links externos.
2. Propagar falhas de bootstrap de sessÃ£o, encerrar cedo lotes sem fila e tornar o teardown do app idempotente para todos os caminhos de saÃ­da.
3. Classificar manutenÃ§Ã£o/acesso negado sem contaminar o kill-switch e validar os schemas persistidos do renderer.
4. Dividir o serviÃ§o Playwright por responsabilidade, remover `autoSync`/`sqlite`, corrigir o README e estabelecer um limite para warnings.
