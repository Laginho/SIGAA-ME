# SIGAA-ME Hardening Tracker

Este tracker foi movido para `.scratch/` em 2026-09-03; este arquivo fica só para que links antigos resolvam.
Convenção: um diretório por fase (`.scratch/NN-faseN-slug/`), com `spec.md` (seção da fase em `docs/PLANO.md`) e `ledger.md` (tarefas fechadas).
Uma tarefa é um arquivo em `issues/NN-ID-slug.md`, com a linha `Status:` (open/claimed/resolved/blocked) no topo e o corpo original do tracker abaixo.
O incidente `SEC-000` está em `.scratch/00-incidente/`; os débitos aceitos em `.scratch/06-debitos-aceitos/`.
As seções transversais retiradas (handoff, verification ledger, task change log) estão no histórico: `git show c9ebaa3:docs/HARDENING_TRACKER.md`.
