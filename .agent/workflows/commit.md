---
description: Commit changes to git
---

**IMPORTANT: Always commit after making code changes!**

1. Check status
// turbo
2. Add all changes
// turbo
3. Commit with message

**Reminder:** After EVERY successful code change (verified by `npx tsc --noEmit`), immediately commit the changes. Do not wait for user to ask.

**Be careful:** DO NOT run add and commit commands at once. Do it separately.